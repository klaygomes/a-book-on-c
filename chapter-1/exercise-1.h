#ifndef EXERCISE_1_H
#define EXERCISE_1_H

#include <sys/types.h>

void print_string_on_one_line(char s[], size_t len);

void print_string_on_three_lines(char s[], size_t len);

void print_string_inside_box(char s[], size_t len);

#endif
